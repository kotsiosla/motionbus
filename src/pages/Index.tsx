import { LiveBusMap } from "@/components/LiveBusMap";

const Index = () => {
  return (
    <main className="min-h-screen bg-gradient-to-b from-background via-background to-muted/30 p-6">
      <LiveBusMap />
    </main>
  );
};

export default Index;
